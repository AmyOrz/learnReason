{
  "name": "wonder-reason-test",
  "version": "1.0.0",
  "repository": "git@github.com:DreamForeast/Wonder-Reason-Test.git",
  "license": "MIT",
  "scripts": {
    "watch": "bsb -make-world -w",
    "build": "bsb -clean-world -make-world",
    "test": "npm run build && jest --config jest.json",
    "test:watch": "jest --watch --config jest.json"
  },
  "devDependencies": {
    "babel": "^6.23.0",
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "bs-jest": "^0.2.0",
    "bs-platform": "^1.9.3",
    "jest": "^21.2.1",
    "jest-cli": "^21.2.1",
    "sinon": "^4.0.1"
  },
  "dependencies": {
    "express": "^4.16.1",
    "http": "^0.0.0"
  }
}
